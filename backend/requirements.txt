fastapi
uvicorn[standard]
pydantic
python-dotenv

# Vector DB + retrieval
qdrant-client
rank-bm25

# Embeddings + reranking
FlagEmbedding
sentencepiece
transformers>=4.44,<5.0
tokenizers

# CPU-only torch build -- the default PyPI "torch" package bundles the full
# NVIDIA CUDA toolkit inside the wheel (multi-GB), which this deployment
# doesn't need since inference runs on CPU here. The +cpu version suffix
# forces pip to fetch specifically from PyTorch's CPU-only index below,
# rather than ambiguously resolving to the CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu

# LLM (Groq via LlamaIndex)
llama-index-core
llama-index-llms-groq

# Tracing
langsmith

# Evaluation (Ragas + pinned compatible langchain stack -- see project notes
# on why these versions are pinned: newer ragas has a broken import against
# newer langchain-community)
ragas==0.1.21
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.43
langchain-groq==0.1.9
langchain-huggingface==0.0.3
langchain-openai==0.1.25
datasets


# fastapi
# uvicorn[standard]
# pydantic
# python-dotenv

# # Vector DB + retrieval
# qdrant-client
# rank-bm25

# # Embeddings + reranking
# FlagEmbedding
# sentencepiece
# transformers>=4.44,<5.0
# tokenizers
# torch

# # LLM (Groq via LlamaIndex)
# llama-index-core
# llama-index-llms-groq

# # Tracing
# langsmith

# # Evaluation (Ragas + pinned compatible langchain stack -- see project notes
# # on why these versions are pinned: newer ragas has a broken import against
# # newer langchain-community)
# ragas==0.1.21
# langchain==0.2.16
# langchain-community==0.2.16
# langchain-core==0.2.43
# langchain-groq==0.1.9
# langchain-huggingface==0.0.3
# langchain-openai==0.1.25
# datasets