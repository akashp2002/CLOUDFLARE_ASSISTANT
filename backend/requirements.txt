fastapi
uvicorn[standard]
pydantic
python-dotenv

# Vector DB + retrieval
qdrant-client
rank-bm25

# Embeddings + reranking
FlagEmbedding
sentencepiece
transformers>=4.44,<5.0
tokenizers
torch

# LLM (Groq via LlamaIndex)
llama-index-core
llama-index-llms-groq

# Tracing
langsmith

# Evaluation (Ragas + pinned compatible langchain stack -- see project notes
# on why these versions are pinned: newer ragas has a broken import against
# newer langchain-community)
ragas==0.1.21
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.43
langchain-groq==0.1.9
langchain-huggingface==0.0.3
langchain-openai==0.1.25
datasets