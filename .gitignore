# Secrets -- never commit this
backend/.env

# Python virtual environment
venv/

# Python cache
__pycache__/
*.pyc
backend/__pycache__/

# Generated data -- NOTE: backend/data/embeddings IS committed (not ignored),
# since Railway's build has no persistent volume like local Docker Compose --
# the embeddings file must be present in the repo for the image build to COPY it in.
qdrant_storage/

# Node / React
frontend/node_modules/
frontend/dist/

# OS/editor junk
.vscode/
.idea/
Thumbs.db
.DS_Store